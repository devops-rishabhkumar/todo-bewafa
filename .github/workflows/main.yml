name: Hello World Workflow

on: 
  workflow_dispatch: 
    inputs:
      infra_env:
        description: 'Where'
        type: string
        default: dev

jobs:
  my_first_job:
    name: My first job
    runs-on: ubuntu-latest
    # env:
    #   NAME: 'Rishabh Kumar Bhatnagar'

    steps:
      # - name: Checkout
      #   uses: actions/checkout@v6.0.2
        #continue-on-error: true
      - name: Greeting
        run: echo "${{ github.event.inputs.infra_env }} Pipeline has been created by ${{ vars.NAME }} on ${{ RUNNER.OS }} Machine";df -h

  # my_second_job:
  #   name: My second job
  #   runs-on: windows-latest
  #   #needs: my_first_job
  #   steps:
  #     - name: Greeting
  #       run: write-host "Hello World"
