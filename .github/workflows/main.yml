name: Hello World Workflow

on: workflow_dispatch


jobs:
  my_first_job:
    name: My first job
    runs-on: ubuntu-latest
    # env:
    #   NAME: 'Rishabh Kumar Bhatnagar'

    steps:
      # - name: Checkout
      #   uses: actions/checkout@v6.0.2
        #continue-on-error: true
      - name: Greeting $RUNNER_OS ${{ RUNNER.OS }}
        run: echo "Hello World from ${{ vars.NAME }}!"

  # my_second_job:
  #   name: My second job
  #   runs-on: windows-latest
  #   #needs: my_first_job
  #   steps:
  #     - name: Greeting
  #       run: write-host "Hello World"
